<h1><%= t("page.headline.edit") %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t("page.link.show"), @book %> |
  <%= link_to t("page.link.back"), books_path %>
</nav>
