<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t("page.link.edit"), edit_book_path(@book) %> |
  <%= link_to t("page.link.back"), books_path %>

  <%= button_to t("page.button.destroy"), @book, method: :delete %>
</nav>
